We create 'b' with bitsets up to size 3010. Then, we calculate the number of shared set bits between pairs of bitsets using bitwise AND (&) and the count() method. We then sum these counts for all pairs, calculating their combined contribution.

Time Complexity : O(n^2)
Space Complexity : O(n^2)